---
description: Save a durable Ginko memory about the current work.
---

Save a concise durable memory using llm-memory.

If the type is not obvious, ask the user. Prefer `decision`, `fact`, or `preference`.

Call `memory_remember` with enough context that a future coding session can act on it without rediscovery.
